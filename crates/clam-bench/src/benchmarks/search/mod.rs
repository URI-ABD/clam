//! Search benchmarks

use crate::Args;

/// Run the search benchmarks.
#[allow(clippy::unnecessary_wraps)]
pub fn search(args: &Args) -> Result<(), String> {
    println!("Running search benchmarks with args: {args:?}");

    Ok(())
}
