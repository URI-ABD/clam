[workspace]
members = [
    "abd-clam",
    # "py-clam",  # TODO: re-enable when py-clam uses maturin to bing to abd-clam
    "distances",
    "rust-automl",
    "simd-euclidean",
    "SyMaGen"
]

[profile.test]
opt-level = 3
debug = true
overflow-checks = true

[profile.release]
strip = true
lto = true
codegen-units = 1

[profile.bench]
debug = true
overflow-checks = true
